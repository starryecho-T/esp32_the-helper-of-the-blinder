# 协作指南

1. 开始前同步 `main` 分支。
2. 使用清晰的任务分支，例如 `feature/cane-ultrasonic`、`feature/scout-espnow` 或 `docs/communication-protocol`。
3. 每次提交只处理一个明确事项，并写清楚提交说明。
4. Push 后创建 Pull Request；合并前确认代码可编译、协议一致且没有敏感信息。
5. `.aia` 文件由同一时间的一位成员维护；编辑前在 `app/APP_EDITING_LOCK.md` 登记。
